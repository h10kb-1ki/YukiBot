name: Keep Alive Ping

on:
  schedule:
    - cron: "*/10 * * * *"  # 10分毎に実行
  workflow_dispatch:   # 手動実行も可能

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Render
        run: curl -s https://yukibot-hd2c.onrender.com/ > /dev/null